enum ActiveStatus {
    ACTIVE
    INACTIVE
}

enum Role {
    USER
    AUTHOR
    ADMIN
}


enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}

enum CommentStatus {
    PENDING
    APPROVED
    REJECTED
}